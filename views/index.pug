extends layout

block layout-content
    div.View
        h1.Banner TinderBot by Edofo
        ul.Message
            script
                | var species = !{JSON.stringify(data)};
                | console.log(species)
            each val in data
                li= val.name
            else
                li There are no values
        div.Title
            h3 Making the Best
            h1 Food For Devs
        span.Details Access the WHATABYTE Team Portal
        div.NavButtons
            a(href="/user") test
            div.NavButton Just dive in!